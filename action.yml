name: 'Hello World'
description: 'Greet someone'
inputs:
  url:  
    description: 'url'
    required: false

  token:  
    description: 'token'
    required: false

  stack:  
    description: 'stack'
    required: true
outputs:
  random-number:
    description: "saida do deploy"
    value: deployada
runs:
  using: "composite"
  steps:
    - run: ${{ github.action_path }}/test/goodbye.sh
      shell: bash
